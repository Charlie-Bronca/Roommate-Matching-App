extends layout
block content
    - console.log(data)
    .main-content 
    h1 FlatBuddies Test
    table(border=1)
        for item in data 
            tr  
                td #{item.user_id}
                td #{item.first_name + item.last_name}